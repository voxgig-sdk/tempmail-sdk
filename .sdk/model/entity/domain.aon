# Entity: domain

main: kit: entity: domain: {

  alias: field: {}

  fields: [
    {
      name: "domains"
      req: false
      type: "`$ARRAY`"
    }
  ]

  name: "domain"
  op: {
    list: {
      name: "list"
      points: [
        {
          method: "GET"
          orig: "/domains"
          parts: [
            "domains"
          ]
          transform: {
            req: "`reqdata`"
            res: "`body.domains`"
          }
        }
      ]
    }
  }



}
